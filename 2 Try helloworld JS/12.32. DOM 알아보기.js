* DOM(Document Object Model)이란 컴퓨터가 문서를 잘 처리할 수 있도록 문서의 구조를 약속한 것
* DOM은 트리 형태 (족보, 가계도)
* document 객체는 최상위 객체로 자바스크립트에서 document 명령으로 접근 가능

----------------------------------------------------------------------

* document.getElementBy() : HTML코드에서 id가 기록되어 있는 특정 태그를 얻어오는 메서드
* document.getElementsBy(); 위 복수형
.innerHTML 엘리먼트 안의 HTML 코드를 변경 하는 속성
.style 속성은 CSS를 변경할 수 있음
* getAttribute(), setAttribute() 메서드는 엘리먼트의 속성 값을 얻어옴 <get은 인자 한개(속성이름) set은 인자 두개(속성 이름과 속성값)>
(src와 달리 input 태그의 값 속성은 getAttribute 메서드로 잘 동작하지 않고 null 이라고 표시)
----------------------------------------------------------------------

