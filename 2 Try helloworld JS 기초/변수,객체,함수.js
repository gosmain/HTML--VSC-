var a = b; // 변수

var man = {name: "홍길동", age: 20, height: 180}; //객체(하나의 변수 속의 여러 복잡한 정보)

function Sum(arg1, arg2) {   //(자바의 메서드)
    var result = arg1 +arg2;
    return result;
} //함수 정의하기

Sum(1,2); //위 함수 호출하기

배열: var abc = [1, 2, 3, 4, 5];