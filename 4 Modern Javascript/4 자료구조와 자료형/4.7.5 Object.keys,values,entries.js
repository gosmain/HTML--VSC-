반복(iteration)
// 주요 메서드 : .keys(), .values(), .entries()

Object.keys(obj);    // 객체의 키만 담은 배열을 반환
Object.values(obj);  // 객체의 값만 담은 배열을 반환
Object.entries(obj); // [키, 값] 쌍을 담은 배열을 반환

// 위 세 메서드를 쓸 수 있는 자료구조 :
Map, Set, Array;

